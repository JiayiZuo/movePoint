Nothing should be ignored in this case, as the added files are all image assets for a miniprogram, which are legitimate source files that should be tracked in version control. No build artifacts, dependencies, or temporary files are present in the changes.

(empty)